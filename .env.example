# Docker Compose Environment Variables
# Este archivo contiene variables globales para docker-compose.yml
# Copia este archivo como .env para configurar el entorno de Docker

# Puertos externos (host machine)
FRONTEND_PORT=3000
BACKEND_PORT=5000
MONGODB_PORT=27017

# URLs de conexión entre servicios
BACKEND_URL=http://localhost:5000
FRONTEND_URL=http://localhost:3000

# Environment
NODE_ENV=development
